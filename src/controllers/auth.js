const login = async (req, res) => {
  // get the user name and password from the body
  // check if username exist in the database
  // if the user exists, get the information from the database
  // chech the password coming from the body with what stored in database
  // if username and password matches, save user's session.
};

module.exports = {
  login,
};
